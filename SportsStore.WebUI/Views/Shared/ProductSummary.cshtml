@model SportsStore.Domain.Product

<div class="item">
    <h3>@Model.Name</h3>
    @Model.Description
    <h4>@Model.Price.ToString("c")</h4>
    <button name="add" type="submit" value="@Model.ProductID">
        Add to Cart
    </button>
</div>